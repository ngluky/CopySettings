using CopySettings.Obje;

namespace CopySettings.Hellp
{
    public static class RenderSetting
    {
        public static Data RenderKeybind(string characterName)
        {
            var data = Constants.GetNewData();
            foreach (var i in data.actionMappings)
            {

            }

            return null;
        }

    }
}
